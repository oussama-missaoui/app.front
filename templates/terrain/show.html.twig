{% extends 'terrain/index.html.twig' %}

{% block title %}Terrain{% endblock %}

{% block body %}
    <div class="background">
        <br><br>
        <br>
<div  class="contact" >
    <div class="container">
        <div class="card" >

        <h1>Terrain {{ terrain.id }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th class="table-info">Id</th>
                <td>{{ terrain.id }}</td>
            </tr>
            <tr>
                <th class="table-info">Type</th>
                <td>{{ terrain.type }}</td>
            </tr>
            <tr>
                <th class="table-info">Dimension</th>
                <td>{{ terrain.dimension }}</td>
            </tr>
            <tr>
                <th class="table-info"  >Adress</th>
                <td>{{ terrain.adress }}</td>
            </tr>
            <tr>
                <th class="table-info" >Capacite</th>
                <td>{{ terrain.capacite }}</td>
            </tr>
            <tr>
                <th class="table-info">Prix</th>
                <td>{{ terrain.prix }}</td>
            </tr>
            <tr>
                <th class="table-info">Duree</th>
                <td>{{ terrain.duree }}</td>
            </tr>
            <tr>
                <th class="table-info">Image</th>
                <td class="text-center"><img width="500" height="300" src="{{ asset('images/Terrains/') }}{{ terrain.imageName }}" class="rounded" alt="terrain"></td>
            </tr>
        </tbody>
    </table>
        </div>
    <a class="btn btn-dark mt-3" href="{{ path('app_terrain_index') }}">Back to list</a>

    <a  class="btn btn-info mt-3" href="{{ path('app_terrain_edit', {'id': terrain.id}) }}">Edit</a>
<br> <br>
    {{ include('terrain/_delete_form.html.twig') }}

    </div>
</div>
        <br>
    </div>
{% endblock %}
