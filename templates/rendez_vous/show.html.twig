{% extends 'rendez_vous/index.html.twig' %}

{% block title %}RendezVous{% endblock %}

{% block body %}
<div class="background">

<br>
    <br>
    <br>
<div  class="contact">
    <div class="container">
        <div class="card" >
    <h1>Reserved Terrain by :  </h1>
            <h3>{{ rendez_vou.user.nom }} {{ rendez_vou.user.prenom }}</h3>

    <table class="table">
        <tbody>
            <tr>
                <th class="table-info">Id</th>
                <td>{{ rendez_vou.id }}</td>
            </tr>
            <tr>
                <th class="table-info">Type</th>
                <td>{{ rendez_vou.terrain.type  }}</td>
            </tr>
            <tr>
                <th class="table-info">Terrain</th>
                <td>{{ rendez_vou.terrain.adress  }}</td>
            </tr>
            <tr>
                <th class="table-info">Date</th>
                <td>{{ rendez_vou.date ? rendez_vou.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th class="table-info">Heure</th>
                <td>{{ rendez_vou.heure ? rendez_vou.heure|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>
        </div>
        <div class="btn-group mt-3" role="group" aria-label="Basic example">

    <a class="btn btn-dark" href="{{ path('app_rendez_vous_index') }}">back to list</a>

    <a class="btn btn-secondary" href="{{ path('app_rendez_vous_edit', {'id': rendez_vou.id}) }}">edit</a>

    {{ include('rendez_vous/_delete_form.html.twig') }}
    </div>
</div>
</div>
    <br>
    <br><br><br><br><br><br>
    <br>
</div>
{% endblock %}
